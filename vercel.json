{
  "version": 2,
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "python -c \"import os; os.makedirs('staticfiles', exist_ok=True)\" && python manage.py collectstatic --noinput && python manage.py migrate --noinput",
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "api/index.py" }]
}
